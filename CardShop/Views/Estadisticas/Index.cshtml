@model IEnumerable<CardShop.Models.Estadistica>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CantBebida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CantPostre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CantComida)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CantBebida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantPostre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantComida)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
